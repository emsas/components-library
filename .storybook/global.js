import { createGlobalStyle } from "styled-components";

export default GlobalStyle = createGlobalStyle`
  name: "DARK",
  body {
    font-family: "Inter", "Open Sans" "Roboto", "Ubuntu", sans-serif;
  }
`;
